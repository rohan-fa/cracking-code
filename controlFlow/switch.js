// for redux use switch used the most

const month = 3 //in the condition we need to work with month so key is that

switch (month) {
    case 1:             //if the month is equil to the given number?  
        console.log("January");
        break;
    case 2:             //if the month is equil to the given number?  
        console.log("feb");
        break;
    case 3:             //if the month is equil to the given number?  
        console.log("march");
        break;
    case 4:             //if the month is equil to the given number?  
        console.log("april");
        break;

    default:
        console.log("sanhdio");
        break;
}